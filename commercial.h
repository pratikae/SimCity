#include "simulation.h"

void commercial(vector< vector<int> > &copyVec, vector< vector<string> > &originalVec); //initialize commercial function
int adj_population(vector< vector<int> > &copyVec, vector< vector<string> > &originalVec, int i, int j); //initialize function to check adjacent population
